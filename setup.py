from setuptools import setup

setup(
    name='experiments',
    version='0.1.0',    
    description='Library to help logging data from experiments and querying the results',
    author='Jacob Springer',
    packages=['experiments'],
    install_requires=[],
    classifiers=[],
)